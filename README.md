# Portfolio Starter (React + Vite + Tailwind)

Drop-in portfolio for recruiters. Clean, fast, and easy to edit.

## Quick start

```bash
npm i
npm run dev
```

Place your résumé at `public/resume.pdf` (there's a placeholder).  
Edit `src/projects.ts` with real links (Live, GitHub, Loom).
